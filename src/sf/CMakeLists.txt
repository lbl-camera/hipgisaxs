
TARGET_SOURCES(
    hipgisaxs
    PRIVATE
	${CMAKE_CURRENT_LIST_DIR}/liquid.cpp
	${CMAKE_CURRENT_LIST_DIR}/sf.cpp 
)


IF (CUDA_FOUND)
    ADD_SUBDIRECTORY(gpu)
    ADD_DEFINITIONS(-DSF_GPU)
ENDIF(CUDA_FOUND)
